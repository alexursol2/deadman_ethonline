# ETHGlobal Project Check-in #2

**Due:** Friday 11 September, 05:59. Post Thursday night.
**Track:** Hedera, AI & Agentic Payments. Second partner: Privy.

The form is field by field, not one block of text. Each heading below is a field on the form.
Paste them in order.

---

## Short description (max 100 characters)

```
An x402 escrow whose refund is executed by the Hedera network itself. No keeper, no arbiter.
```

92 characters. Shorter alternative if the field complains, 86:
`An x402 escrow whose refund the Hedera network executes itself. No keeper, no arbiter.`

---

## Any blockers? Need help?

```
Two, both on the partner side, both carried over from check-in #1.

1. Our Hedera questions are still unanswered, and we now have six. Two of them are gaps in
Hedera's own stack rather than ours. (a) x402 on Hedera cannot settle into a contract call:
@x402/hedera only builds a TransferTransaction and the facilitator rejects anything else by
name, so no escrow, vault or streaming product on that rail can take payment atomically, and
we can point at the exact line. (b) Network-executed calls do not appear under the mirror
node's /contracts/{address}/results, so a scheduled call that fired and reverted is invisible
where any dashboard would look. (c) A rejected deleteSchedule returns transaction SUCCESS with
the refusal only in the return code, and Hedera's own payments-scheduler template ships a
helper that discards it. (d) scheduleCall reverts with empty returndata when starved of gas,
which contradicts how HIP-1215's "never reverts" reads, with a measured floor around 1.45M
gas. (e) The delete authorisation model is undocumented, so we established it empirically.
(f) hasScheduleCapacity(now + 1) returns false despite the HIP stating a one second minimum.
We would like these in front of a Hedera engineer before Sunday.

2. Blocky402 is a single vendor hard gate. It is up and has behaved correctly in every test,
but it is one hosted service with no public source and no status page, and settling through it
is a track requirement. If it goes down during judging we cannot substitute it and we fail
qualification for reasons outside our control. Is there a contact, or a fallback position?

Not blocked on anything of our own.
```

---

## What progress did you make since the last check-in?

```
It is live and it works end to end in public: https://deadman-server.onrender.com is the x402
endpoint and https://deadman-board.onrender.com is a live board. A real x402 payment settles
through Blocky402 into our escrow contract (0.0.10426758), the escrow arms its own refund with
the Hedera network in the next transaction, and if the seller never reveals the decryption key
the network executes that refund on its own. The refund transaction carries signatures: [],
which anyone can check on the mirror node. No keeper, no cron, nobody pushing a button.

Finished since check-in #1: the escrow contract, with 53 adversarial tests where every test
was proven to fail against a deliberately broken build before it counted; the x402 resource
server and a paying agent; and verify.ts, a buyer side tool that catches a lying seller and
names which of four commitments was broken, demonstrated against three real cheats where the
chain is happy and the seller is paid.

Privy is integrated. The agent's signing key does not exist on any machine of ours, it is a
Privy server wallet, and because x402 on Hedera pays with a native TransferTransaction rather
than an EVM one we sign it through Privy's raw secp256k1 primitive. We also report the
negative result: the spend policy cannot bind this rail, because raw signing has no policy
method and the wildcard that permits it also voids the cap. It is measured and written up
rather than glossed.

Our own adversarial suite found a real vulnerability in our contract along the way. It is
fixed and written up rather than quietly patched.
```

---

## Is there anything else you think we should know?

```
We would rather be judged on what we can prove than on what we claim, so the limits are on the
first screen of the README, not buried. We guarantee delivery, not correctness: a seller can
encrypt garbage, commit to its hash, and get paid. That is an oracle problem and we say so.
The refund is armed one transaction after the money lands rather than atomically, because
Hedera cannot do better today, and that is blocker 1(a). One code path, capacity saturation,
has only ever run against a mock and we label it that way instead of implying coverage we do
not have.

The README also carries a prior art table naming the closest competing designs, including
Boson Protocol's x402B and Held, which a builder from another ecosystem pointed us to this
week. In those, silence pays the seller and the buyer must dispute to stop it, or a hosted
watchdog raises the dispute for them. Ours inverts that: silence refunds the buyer, and there
is no service to run.
```

---

## Radio buttons and select

| Field | Answer |
|---|---|
| Will you be joining us for Live Judging to qualify for Top 10 Finalist? | **Yes** |
| Are you on track for submitting a project? | **Yes** |
| Are there any prizes in particular you're going for? | **Hedera** (AI & Agentic Payments) and **Privy** |

Live judging is a real commitment to be present at a scheduled time. Answer Yes only if the slot
is actually free. Saying Yes and not showing up is worse than saying No.

---

## Notes for us, not for the form

- Blocker 1 is stronger than in check-in #1 because two of the six are now Hedera's problem, not
  ours. That is the kind of thing a partner wants to hear before a judge finds it.
- Do not soften the vulnerability sentence. Finding a bug in our own contract and writing it up is
  evidence the rest is measured. Hiding it would be the opposite.
- The prior art paragraph exists because naming the competition is more convincing than not being
  aware of it.
